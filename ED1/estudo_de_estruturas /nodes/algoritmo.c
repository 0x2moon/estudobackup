// *cria struct de nó
// *cria struct de lista de nos
// *cria função para alocar uma lista de nos
// cria uma função para inserir dentro da struct do final da lista de nos
    // uma função que recebe como parametro (a lista de nos)
    // crie um laço de repetição "do while" que vai durar enquanto o campo next dentro do Nó for diferente de nulo.
    // quando o laço 'do while' acabar, insere no campo nome daquele nó
// cria uma função para buscar um valor na lista de nos
    // crie um laço de repetição "do while" que vai durar enquanto o campo nome for diferente da string que voce quer procurar.
    // a cada ciclo do laço, a variavel next deve ser consultada, se for diferente de nulo, o pronteiro de referencia deverá apontar para a proxima estrutura.
    // caso seja nulo, retorne que não está na lista a string.
